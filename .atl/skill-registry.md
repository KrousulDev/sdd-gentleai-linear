# Skill Registry

## Metadata
- Project: sdd-gentleai-linear
- Generated by: /sdd-init
- Source status: native OpenCode TypeScript plugin repository with runtime, tests, and docs

## User Skills Trigger Table
| Skill | Trigger | Scope |
| --- | --- | --- |
| none | No project-local skills detected | project |

## Compact Rules

### Project Standards (docs-derived)
- El workflow SDD lo gobierna gentle-ai/OpenCode; no crear un segundo orquestador.
- Engram es la fuente canónica de estado; Linear es espejo operativo.
- El plugin objetivo del MVP debe ser nativo de OpenCode y escrito en TypeScript.
- `verify` y `archive` son gates duros; no deben tratarse como sugerencias de UX.
- El task splitting hacia child issues debe permanecer en `mode=ask`.
- El MVP no debe implementar sync inverso desde Linear hacia el estado canónico SDD.
- Los commits finales deben preservar trazabilidad con dual-ID (`linearIssueId` + `sddId`).

## Project Conventions
- El proyecto activo implementa un plugin nativo de OpenCode en TypeScript bajo `.opencode/plugins/linear-plugin-mvp.ts`.
- La fuente canónica SDD sigue siendo Engram; Linear es espejo operativo one-way.
- El repo tiene validación local con `npm run typecheck`, `npm test` y `npm run test:coverage`.

## Notes
- Este registro ya no debe tratar al repo como docs-only.
- Si cambian runtime, tooling o convenciones, este archivo debe refrescarse junto con `sdd-init` y `testing-capabilities` en Engram.
